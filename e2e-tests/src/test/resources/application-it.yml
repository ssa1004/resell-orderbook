spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  cache:
    type: simple
  kafka:
    bootstrap-servers: ""

market:
  pg:
    enabled: false
  bank:
    enabled: false
  storage:
    s3-enabled: false
  cache:
    redis-enabled: false
  security:
    jwt:
      enabled: false
  advisory-lock:
    enabled: false           # Postgres 지만 e2e 단순화 — 단일 thread
  outbox:
    relay:
      enabled: false         # Kafka 비활성 — Outbox 만 INSERT 검증
  trade:
    payment-ttl-minutes: 15
  fee:
    currency: KRW
    seller-commission-rate: 3.0
    buyer-commission-rate: 3.5
    inspection-fee: 3000
    shipping-fee: 3000
    fixed-processing-fee: 1000
